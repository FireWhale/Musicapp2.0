<table>
  <tr>
    <th>Name</th>
    <th>Release Date</th>
    <th>Album Obtained?</th>
  </tr>

<% @sorted.each do |each| %>
  <tr>
    <td><%= link_to each.name, each %></td>
    <td><%= each.releasedate %></td>
    <td><%= each.albumobtained %></td>
  </tr>
<% end %>
</table>

<br />